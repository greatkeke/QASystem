@using QASystem.Core.Domain;
@{
    foreach (Subject sub in ViewBag.Cates)
    {
        <li>@Html.ActionLink(linkText: sub.Name, actionName: "Subject", controllerName: "Question", routeValues: new { subjectId = sub.Id }, htmlAttributes: null)</li>
    }
}